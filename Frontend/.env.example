# Copy to `.env.local` for local development overrides (Vite)
# Default API base is set in src/utils/apiBase.js

# Local backend:
# VITE_API_BASE_URL=http://localhost:8000

# Deployed backend (Render):
VITE_API_BASE_URL=https://forensight-criminal-face-generation-and.onrender.com
